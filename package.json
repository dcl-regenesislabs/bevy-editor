{
  "name": "dcl-editor",
  "version": "0.1.0",
  "private": true,
  "description": "Decentraland scene editor — monorepo (in-world web + Electron desktop). The engine (bevy-explorer) is an external dependency consumed as a wasm build.",
  "workspaces": [
    "packages/contract",
    "packages/scene",
    "packages/ui",
    "packages/desktop"
  ],
  "engines": {
    "node": ">=22",
    "npm": ">=10"
  },
  "scripts": {
    "build": "npm run build -w @dcl-editor/scene && npm run build -w @dcl-editor/ui && npm run build:main -w @dcl-editor/desktop",
    "build:ui": "npm run build -w @dcl-editor/ui",
    "build:scene": "npm run build -w @dcl-editor/scene",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "vitest run",
    "test:watch": "vitest",
    "start": "npm run build && npm run app -w @dcl-editor/desktop",
    "dist": "npm run build && npm run dist -w @dcl-editor/desktop",
    "dev": "node scripts/dev.mjs",
    "validate": "node scripts/validate.mjs",
    "size": "node scripts/measure-size.mjs",
    "size:check": "node scripts/measure-size.mjs --out size-reports/local.json && node scripts/size-check.mjs --measured size-reports",
    "validate:e2e": "npm run build && npm run validate -w @dcl-editor/desktop && npm run validate:probes",
    "validate:probes": "node packages/desktop/validate/probe-script-runner.mjs && node packages/desktop/validate/probe-spawner.mjs",
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "happy-dom": "^20.11.1",
    "typescript-eslint": "^8.0.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@dcl-regenesislabs/bevy-explorer-web": "0.1.0-30459451893.commit-81261ec"
  }
}
