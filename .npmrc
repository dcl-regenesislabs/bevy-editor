; Refuse to install on a Node outside engines (even/LTS-track majors only).
; Odd releases drift from CI — Node 25 shipped a broken default localStorage
; global that shadowed happy-dom's in vitest and killed three suites.
engine-strict=true
