// REPO-ONLY STAND-IN — see ./game.ts. The probe observer reads spawnedFrom() to
// check that the chunks the tower actually placed are the ones the seed asked
// for; in the scene that module arrives with the game module's own closure.
export * from '../../../../../runtime-modules/spawner'
